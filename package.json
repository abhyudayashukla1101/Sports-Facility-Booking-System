{
  "name": "sports-facility-booking-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Campus Sports Facility Booking & Analytics Platform for IIT Guwahati Gymkhana",
  "scripts": {
    "dev": "node scripts/run-all.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run build --prefix frontend && npm start --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
